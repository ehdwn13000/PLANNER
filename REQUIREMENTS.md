# Planner 요구사항 정의서

## 배경
기존 플래너(예: Microsoft Planner)의 다음과 같은 불편함을 해결하기 위한 개인용 웹 플래너.
- 업무 단위가 너무 커서 개별 할 일을 하나하나 체크하기 어려움
- 입력 후 편집이 번거로움 (특히 마감일처럼 자주 바뀌는 정보)
- 종이에 메모하듯 빠르고 간편하게 기록하고 싶음

## 사용 환경
- 개인 전용 (다중 사용자 불필요)
- 웹앱, 모바일 브라우저 접속 위주 (반응형)
- 서버/DB 없이 로컬 저장(localStorage) 기반으로 시작
- 추후 필요 시 PWA 또는 네이티브 앱으로 확장 가능하도록, 로직과 화면(UI)을 분리해서 개발

---

## 1. 데이터 계층 구조

```
[Level 1] 업무
  - 사용자가 자유롭게 생성/수정/이름 지정 (예시일 뿐 고정 카테고리 아님)
  - 마감일 없음, 완료 개념 없음 (분류 목적)

    └─ [Level 2] 프로젝트 (진행 중인 중요 업무 단위)
         - 사용자가 자유롭게 생성/수정
         - 완료 처리 가능 (아래 "Level 2 완료 처리" 참조)

              └─ [Level 3] 실행 항목 (실제 체크하는 최소 단위 할 일)
                   - 마감일
                   - 완료 체크 (O/X)
                   - 중요도: 상 / 중 / 하
```

### Level 2 완료 처리 방식 (수동)
- 하위 Level 3 항목이 모두 완료되지 않아도, 사용자가 직접 "프로젝트 완료" 처리를 할 수 있음
- 자동 완료 처리는 하지 않음 (사용자가 명시적으로 완료 버튼을 눌러야 함)

### 완료 항목 처리 공통 규칙
- Level 2 또는 Level 3 항목을 완료 처리하면 **활성 화면(기본/오늘 할 일/급한 일/주간 뷰)에서 사라짐**
- 완전 삭제되지 않고 **히스토리(기록)**에 보관됨
- 히스토리는 **Level 1 → Level 2 → Level 3 계층 구조를 그대로 유지**한 채로 조회 가능 (어느 업무의 어느 프로젝트에 속했던 항목인지 알 수 있어야 함)

---

## 2. Level 3 실행 항목 속성
| 속성 | 설명 |
|---|---|
| 이름 | 할 일 내용 |
| 마감일 | 자주 바뀌므로 인라인으로 즉시 수정 가능해야 함 |
| 완료 체크 | O/X. 미체크 상태는 자동으로 "진행중"을 의미 (별도 상태값 없음) |
| 중요도 | 상 / 중 / 하. 인라인으로 즉시 변경 가능해야 함 |

---

## 3. 화면별 요구사항

| 화면 | 내용 |
|---|---|
| 기본 화면 | 업무(Level 1)별로 그룹핑하여 표시. 하위 미완료 실행항목과 마감일 표시. 업무/프로젝트 하위 완료율(%) 표시 |
| 오늘 할 일 | 오늘이 마감이거나 처리해야 할 실행항목만 모아보기 |
| 급한 일 보기 | Level 3 실행항목만 대상 (Level 1/2는 분류일 뿐 "할 일"이 아니므로 제외). 마감 임박 순 정렬. 미완료 항목만 |
| 주간 뷰 | 이번 주 마감인 항목 + 진행중(미완료)인 항목을 함께 표시. 마감/진행중 색상 구분. 마감이 지난(overdue) 항목은 별도 색으로 강조 |
| 업무별 기록(히스토리) | 완료 처리된 Level 2/Level 3 항목을 Level 1 → Level 2 → Level 3 구조 그대로 조회 |

---

## 4. 빠른 메모 / 체크리스트 (업무 계층과 별도)
- 계층 없는 단순 체크리스트
- **개인 / 업무(기타)** 두 가지 태그로 구분
- 용도 예: "급하게 파일 하나 보내주기" 같이 구조화된 업무 계층에 넣기 애매한, 잊으면 안 되는 잡무나 개인 용무

---

## 5. 편집 편의성 (핵심 요구사항)
- 실행 항목 입력/수정은 종이에 메모하듯 빠르고 간편해야 함 (클릭 최소화)
- 마감일, 중요도는 별도 화면 이동 없이 **인라인으로 즉시 수정** 가능해야 함

---

## 6. 참고 - 다음 단계에서 결정할 사항
- MVP 범위 확정 (어떤 기능부터 만들지) → `FEATURES.md`
- 데이터 스키마 상세 설계 → `ARCHITECTURE.md`
- 기술 스택 확정
